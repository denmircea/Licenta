import React from 'react';
import { View } from 'react-native';
//import MapView from 'react-native-maps';

export const NativeMapView: React.FC = () => {
    // Replace with your native map implementation
    return (
        <View>
            {/* <MapView
                style={{ width: '100%', height: '100%' }}
                initialRegion={{
                    latitude: 44.4268, // Example coordinates for Bucharest
                    longitude: 26.1025,
                    latitudeDelta: 0.01,
                    longitudeDelta: 0.01,
                    }}
                    showsUserLocation={true}
                    showsMyLocationButton={true}
                    showsCompass={true}
                    showsScale={true}
                    showsTraffic={true}
                    showsIndoors={true}
                    showsBuildings={true}
                >
                
            </MapView> */}
        </View>
    );
};
